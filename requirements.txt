# The web build. The desktop companion adds PySide6 when it exists.
fontTools>=4.50
brotli>=1.1          # woff2, which is what foundries serve
regex>=2024.0.0      # \p{...} answered by the engine itself, not a kept list
